(function(){let forms=document.querySelectorAll('.form-send');if(forms.length===0){return}
let serialize=function(form){let items=form.querySelectorAll('input');let str='';for(let i=0;i<items.length;i+=1){let item=items[i];let name=item.name;let value=item.value;let separator=i===0?'':'&';if(value){str+=separator+name+'='+value}}
return str}
let formSend=function(form){let data=serialize(form);let xhr=new XMLHttpRequest();let url='wp-content/themes/maysternya/mail.php';xhr.open('POST',url);xhr.setRequestHeader('Content-Type','application/x-www-form-urlencoded');xhr.onload=function(){let activePopup=document.querySelector('.popup.open');if(activePopup){activePopup.classList.remove('open')}else{}
if(xhr.response==='success'){document.querySelector('.popup-thnx').classList.add('open')}else{console.log('error')}};xhr.send(data)};for(let i=0;i<forms.length;i+=1){forms[i].addEventListener('submit',function(e){e.preventDefault();let form=e.currentTarget;formSend(form)})}})();(function(){let forms=document.querySelectorAll('.form-send');if(forms.length===0){return}
let serialize=function(form){let items=form.querySelectorAll('input');let str='';for(let i=0;i<items.length;i+=1){let item=items[i];let name=item.name;let value=item.value;let separator=i===0?'':'&';if(value){str+=separator+name+'='+value}}
return str}
let formSend=function(form){let data=serialize(form);let xhr=new XMLHttpRequest();let urltg='wp-content/themes/maysternya/tg.php';xhr.open('POST',urltg);xhr.setRequestHeader('Content-Type','application/x-www-form-urlencoded');xhr.onload=function(){if(xhr.response==='success'){document.querySelector('.popup-thnx').classList.add('open')}else{console.log('error')}};xhr.send(data)};for(let i=0;i<forms.length;i+=1){forms[i].addEventListener('submit',function(e){e.preventDefault();let form=e.currentTarget;formSend(form)})}})()